DECLARE
sno int;
sname varchar(10);
per float;
BEGIN
sno:=&sno;
sname:='&sname';
per:=&per;
insert into student values(sno,sname,per);
END;
/
CREATE TABLE student(sno int primary key,
sname varchar(10),
per float);
