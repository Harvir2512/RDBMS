DECLARE
   v_name VARCHAR2(50);
   n int;

BEGIN
   n:=&n;
   SELECT ename INTO v_name FROM emp WHERE eno = n;
   DBMS_OUTPUT.PUT_LINE('Employee: ' || v_name);

EXCEPTION
   WHEN TOO_MANY_ROWS THEN
      DBMS_OUTPUT.PUT_LINE('Error: Query returned more than one row!');
END;
/